Partner logos used by Login hero + footer on every page.

Required files (drop them into this folder with these exact names):

  lepra.png        LEPRA Society logo  (the teal "LEPRA" badge)
  iihmr.png        IIHMR Bangalore logo (the wordmark with "Institute of Health Management Research")

Recommended:
  * PNG with transparent background, ~600 px wide
  * Or .svg for crisp rendering at any size — if you prefer SVG, rename them
    lepra.svg / iihmr.svg and the components in src/components/PartnerLogos.jsx
    will need their import paths updated accordingly.

Referenced from:
  - src/components/PartnerLogos.jsx
  - served by Vite at the URL paths /logos/lepra.png and /logos/iihmr.png
